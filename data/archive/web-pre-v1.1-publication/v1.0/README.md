# Archived website release v1.0

These files are retained for provenance only. They are superseded by the
frozen `v1.1-rc1` release files in `web/data/` and must not be used for the
current atlas, manuscript tables, or release statistics.

The v1.0 files are not linked from the current Downloads page.
